<mvc:View xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" controllerName="sap.ui.demo.walkthrough.controller.InvoiceList">

    <List headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{path:'invoice>/Invoices',
        filter:[{path:'Status', operator:'EQ', value1:'A'}]}" id="invoiceList">
        <headerToolbar>
            <Toolbar>
                <Title text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer />
                <SearchField width="50%" search=".onFilterInvoices" />
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem title="{invoice>Quantity} x {invoice>ProductName}" number="{
                    path: 'invoice>ExtendedPrice',
                    type: 'sap.ui.model.type.Currency',
                    formatOptions: {
                        showMeasure: true
                    }
                }" icon="{path: 'invoice>Icon'}" intro="{
                    parts:[{path:'invoice>ExtendedPrice/0'},{path:'invoice>Quantity'},{path:'invoice>ExtendedPrice/1'}],
                    formatter: '.formatter.fullPrice'
                }​" numberState="{= ${invoice>ExtendedPrice/0} > 50 ? 'Warning' : 'Success' }" class="invoice">
                <firstStatus>
                    <ObjectStatus text="{
						path: 'invoice>Status',
						formatter: '.formatter.statusText'
					}" />
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>